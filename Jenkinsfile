pipeline {
    agent any
    options {
    }
    environment {
        PATH = "$PATH:$HOME/workspace/ci-tools_master/linux_amd64:$HOME/.poetry/bin"
    }
    stages {
        stage('Build') {
            steps {
                echo "test1"

            }
        }
    }
}
